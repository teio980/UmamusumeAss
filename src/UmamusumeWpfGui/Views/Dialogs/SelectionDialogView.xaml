<ui:FluentWindow x:Class="UmamusumeWpfGui.Views.Dialogs.SelectionDialogView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:vm="clr-namespace:UmamusumeWpfGui.ViewModels.Dialogs"
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance vm:SelectionDialogViewModel}"
                 Title="{DynamicResource SelectionDialogTitle}"
                 Width="520"
                 Height="400"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner"
                 ShowInTaskbar="False"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <ui:TitleBar Grid.Row="0"
                 Title="{DynamicResource SelectionDialogTitle}"
                 ShowMaximize="False"
                 ShowMinimize="False" />

    <StackPanel Grid.Row="1" Margin="32,24,32,16">
      <TextBlock Text="{DynamicResource SelectionDialogTitle}"
                 FontSize="20"
                 FontWeight="Medium"
                 Margin="0,0,0,16" />
      <ListBox ItemsSource="{Binding Items}"
               Background="Transparent"
               BorderThickness="0"
               ScrollViewer.VerticalScrollBarVisibility="Auto"
               ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="Margin" Value="0,0,0,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="Transparent" />
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsSelected}" Value="True">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
          <DataTemplate DataType="vm:SelectionDialogViewModel+SelectableEmulatorItem">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <ui:SymbolIcon Grid.Column="0"
                             Symbol="Circle24"
                             FontSize="20"
                             Margin="0,0,12,0"
                             VerticalAlignment="Center" />
              <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding EmulatorName}"
                           FontWeight="Medium" />
                <TextBlock Text="{Binding AdbPath}"
                           Margin="0,3,0,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="12"
                           TextTrimming="CharacterEllipsis" />
              </StackPanel>
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
    </StackPanel>

    <StackPanel Grid.Row="2"
                Margin="32,0,32,24"
                Orientation="Horizontal"
                HorizontalAlignment="Right">
      <ui:Button Content="{DynamicResource SelectionDialogCancel}"
                 Command="{Binding CancelCommand}"
                 Appearance="Secondary"
                 Margin="0,0,8,0" />
      <ui:Button Content="{DynamicResource SelectionDialogConfirm}"
                 Command="{Binding ConfirmCommand}"
                 Appearance="Primary" />
    </StackPanel>
  </Grid>
</ui:FluentWindow>
