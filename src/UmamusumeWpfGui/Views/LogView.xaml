<UserControl x:Class="UmamusumeWpfGui.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:UmamusumeWpfGui.Converters"
             xmlns:m="clr-namespace:UmamusumeWpfGui.Models"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Foreground="{DynamicResource TextFillColorPrimaryBrush}">
  <UserControl.Resources>
    <c:LogEntryKindToColorConverter x:Key="LogColorConverter" />
    <c:CountToVisibilityConverter x:Key="CountToVisibility" />
  </UserControl.Resources>

  <Grid Margin="42"
        VerticalAlignment="Stretch">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               Text="{DynamicResource TabLog}"
               FontSize="20"
               FontWeight="Medium" />
    <Grid Grid.Row="1"
          Margin="0,12,0,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock Text="{DynamicResource LogDetailsHeader}"
                 VerticalAlignment="Center" />
      <ui:Button Grid.Column="1"
                 Content="{DynamicResource CopyAllLogButton}"
                 Appearance="Secondary"
                 Icon="Copy24"
                 Click="OnCopyAllClick" />
    </Grid>

    <Grid Grid.Row="2">
      <ListBox x:Name="EntriesListBox"
               ItemsSource="{Binding Entries}"
               BorderThickness="0"
               ScrollViewer.HorizontalScrollBarVisibility="Auto"
               ScrollViewer.VerticalScrollBarVisibility="Auto">
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Foreground"
                    Value="{Binding Kind, Converter={StaticResource LogColorConverter}}" />
          </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
          <DataTemplate DataType="m:LogEntry">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <ui:TextBox Grid.Column="0"
                          Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss.fff}}"
                          IsReadOnly="True"
                          BorderThickness="0"
                          Background="Transparent"
                          Padding="0"
                          TextWrapping="NoWrap"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
              <ui:TextBox Grid.Column="1"
                          Text="{Binding Type}"
                          IsReadOnly="True"
                          BorderThickness="0"
                          Background="Transparent"
                          Padding="0"
                          TextWrapping="NoWrap"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
              <ui:TextBox Grid.Column="2"
                          Text="{Binding Details}"
                          IsReadOnly="True"
                          BorderThickness="0"
                          Background="Transparent"
                          Padding="0"
                          TextWrapping="NoWrap"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden" />
            </Grid>
          </DataTemplate>
        </ListBox.ItemTemplate>
      </ListBox>
      <TextBlock Text="{DynamicResource LogEmptyMessage}"
                 FontStyle="Italic"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Visibility="{Binding Entries.Count,
                     Converter={StaticResource CountToVisibility}}"
                 IsHitTestVisible="False" />
    </Grid>
  </Grid>
</UserControl>
