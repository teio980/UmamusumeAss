<UserControl x:Class="UmamusumeWpfGui.Views.Tasks.CareerLegacySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource TextPrimaryBrush}">
  <UserControl.Resources>
    <DataTemplate x:Key="ComboOptionTemplate">
      <TextBlock Text="{Binding Label}" TextWrapping="Wrap" />
    </DataTemplate>
  </UserControl.Resources>
  <StackPanel Margin="8,10">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="108" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <TextBlock VerticalAlignment="Center" Text="Legacy selection" />
      <ComboBox Grid.Column="1"
                Margin="10,0,0,0"
                HorizontalContentAlignment="Stretch"
                ItemsSource="{Binding LegacySelectionModes}"
                ItemTemplate="{StaticResource ComboOptionTemplate}"
                SelectedValuePath="Value"
                SelectedValue="{Binding LegacySelectionMode, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
    <CheckBox Margin="0,8,0,0" IsChecked="{Binding UseCachedLegacy}">
      <TextBlock Text="Use cached Legacy 1 and Legacy 2 records when available" TextWrapping="Wrap" />
    </CheckBox>
    <CheckBox Margin="0,8,0,0" IsChecked="{Binding UseLegacyGuest}">
      <TextBlock Text="Use a guest legacy for Legacy 1" TextWrapping="Wrap" />
    </CheckBox>
    <StackPanel Margin="0,8,0,0" IsEnabled="{Binding IsManualLegacySelection}">
      <TextBlock Text="Attribute sparks" />
      <ItemsControl Margin="0,4,0,0" ItemsSource="{Binding AttributeSparkOptions}">
        <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <CheckBox Margin="0,0,12,4" Content="{Binding Label}" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
      <TextBlock Margin="0,4,0,0" Text="Aptitude sparks" />
      <ItemsControl Margin="0,4,0,0" ItemsSource="{Binding AptitudeSparkOptions}">
        <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <CheckBox Margin="0,0,12,4" Content="{Binding Label}" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </StackPanel>
</UserControl>
