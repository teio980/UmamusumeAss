# Hachimi startup templates

These are real crops from the configured 900x1600 MuMu portrait session:

- `game_home.png`: the stable Home navigation tab.
- `tap_to_start.png`: the `TAP TO START` title-screen control.
- `data_download.png`: the Data Download dialog title.
- `promo_skip.png`: the promotional page skip control.
- `notices_close.png`: the Notices dialog Close button.

Keep templates small and stable. Update the matching task ROI and reference
dimensions together when adapting the flow to another game or device.
